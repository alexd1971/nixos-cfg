{
  xdg.mimeApps = {
    enable = true;

    associations.added = {
      "inode/directory" = [ "thunar.desktop" ];
      "x-scheme-handler/slack" = [ "slack.desktop" ];
    };

    defaultApplications = {
      "inode/directory" = [ "thunar.desktop" ];
      "x-scheme-handler/slack" = [ "slack.desktop" ];
    };
  };
}
